import 'package:flutter/material.dart';

import '../widgets/search_face_form.dart';

class SearchFace extends StatelessWidget {
  const SearchFace({super.key});

  @override
  Widget build(BuildContext context) {
    return const SearchFaceForm();
  }
}
